---
import Base from '../layouts/Base.astro';
import { localizePath } from '../i18n/utils';
import HorseshoeMark from '../components/HorseshoeMark.astro';
const locale = 'pl' as const;
---

<Base title="Dziękujemy — Hovera" description="Otrzymaliśmy Twoje zapytanie. Odpowiemy w 24h." noIndex>
  <section class="container-x py-32 text-center">
    <div class="thanks-mark mx-auto" aria-hidden="true">
      <HorseshoeMark class="h-full w-full text-[var(--color-ochre)]" />
    </div>
    <h1 class="display-2 mt-8">Dziękujemy.</h1>
    <p class="lead mt-5 max-w-xl mx-auto">
      Twoje zapytanie do nas dotarło. Przeczytamy uważnie i odpiszemy w ciągu 24 godzin.
    </p>
    <div class="mt-10 flex flex-wrap items-center justify-center gap-3">
      <a href={localizePath('/', locale)} class="btn btn-primary">Wróć na stronę główną</a>
      <a href={localizePath('/co-nowego', locale)} class="btn btn-secondary">Zobacz, co nowego</a>
    </div>
  </section>
</Base>

<style>
  .thanks-mark { width: 8rem; height: 8rem; animation: spin 18s linear infinite; }
  @keyframes spin { from { transform: rotate(-6deg); } to { transform: rotate(6deg); } }
</style>
